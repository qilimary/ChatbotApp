name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    env:
      ANDROID_HOME: /opt/android-sdk
      ANDROID_SDK_ROOT: /opt/android-sdk
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print env info
        run: |
          uname -a
          df -h /

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc
          sudo apt-get clean
          df -h /

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Verify Java installation
        run: |
          echo "JAVA_HOME=$JAVA_HOME"
          java -version 2>&1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install system deps
        run: |
          sudo apt-get update -y
          sudo apt-get install -y zip unzip python3-pip expect \
            autoconf automake libtool pkg-config zlib1g-dev \
            libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev \
            build-essential ccache git libltdl-dev

      - name: Install buildozer and gdown
        run: |
          python -m pip install --upgrade pip
          pip install "buildozer==1.5.0" "cython==0.29.36" virtualenv "gdown>=4.7.1" "sh>=2.0,<3.0"
          buildozer version

      - name: Setup Android SDK
        run: |
          echo "==== Install Android SDK ===="
          export JAVA_HOME="${JAVA_HOME:-$(dirname $(dirname $(readlink -f $(which java))))}"
          java -version 2>&1

          mkdir -p $ANDROID_HOME/cmdline-tools
          cd /tmp
          wget --no-verbose https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip -d $ANDROID_HOME/cmdline-tools
          mv $ANDROID_HOME/cmdline-tools/cmdline-tools $ANDROID_HOME/cmdline-tools/latest

          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$PATH"
          sdkmanager --version

          echo "==== Pre-write license files ===="
          mkdir -p $ANDROID_HOME/licenses
          printf '\n8933bad161af4177641a525b71e2a3b3\nd56f5187479451eabf01fb78af6dfcb131a6481e\n24333f8a63b6825ea9c5514f83c2829b004d1fee\n' > $ANDROID_HOME/licenses/android-sdk-license
          printf '\nd56f5187479451eabf01fb78af6dfcb131a6481e\n' > $ANDROID_HOME/licenses/android-sdk-preview-license
          printf '\n859f317696f67ef3dab8ee7b7e8bf7b6c1aea6e7\n' > $ANDROID_HOME/licenses/android-sdk-arm-dbt-license
          printf '\nd975f751698a77b662f1254ddbeed3901e7635aa\n' > $ANDROID_HOME/licenses/intel-android-extra-license
          printf '\n33b6a2b64607f11b759f320ef9054a8cd123aaa9\n' > $ANDROID_HOME/licenses/google-gdk-license
          printf '\ne9acab5b5fbb560a72cfaecf894e659d05d8a2ab\n' > $ANDROID_HOME/licenses/mips-android-sysimage-license

          echo "==== Accept licenses with expect ===="
          expect -c '
          set timeout 120
          spawn sdkmanager --licenses
          expect {
              -re "y/N" { send "y\r"; exp_continue }
              "All SDK package licenses accepted" { exit 0 }
              eof { exit 0 }
          }' 2>&1 | tail -5 || true

          echo "==== Install SDK packages ===="
          sdkmanager "platform-tools" "build-tools;34.0.0" "platforms;android-34" 2>&1 | tail -10
          sdkmanager "tools" 2>&1 | tail -5

          echo "==== SDK installed ===="
          ls -la $ANDROID_HOME/

      - name: Patch legacy sdkmanager
        run: |
          BUILD_DIR="$HOME/.buildozer/android/platform/android-sdk"
          mkdir -p "$(dirname "$BUILD_DIR")"
          if [ -L "$BUILD_DIR" ] || [ -d "$BUILD_DIR" ]; then
            rm -rf "$BUILD_DIR"
          fi
          ln -s "$ANDROID_HOME" "$BUILD_DIR"

          LEGACY_SDKMANAGER="$ANDROID_HOME/tools/bin/sdkmanager"
          cat > /tmp/sdkmanager_wrapper.sh << 'WRAPPER'
          #!/bin/bash
          REAL_SDKMANAGER="$(dirname "$(dirname "$(dirname "$0")")")/cmdline-tools/latest/bin/sdkmanager"
          if [ -x "$REAL_SDKMANAGER" ]; then
              exec "$REAL_SDKMANAGER" "$@"
          else
              echo "ERROR" >&2
              exit 1
          fi
          WRAPPER
          chmod +x /tmp/sdkmanager_wrapper.sh
          cp /tmp/sdkmanager_wrapper.sh "$LEGACY_SDKMANAGER" 2>/dev/null || sudo cp /tmp/sdkmanager_wrapper.sh "$LEGACY_SDKMANAGER"
          chmod +x "$LEGACY_SDKMANAGER"

          LEGACY_AVDMANAGER="$ANDROID_HOME/tools/bin/avdmanager"
          cat > /tmp/avdmanager_wrapper.sh << 'WRAPPER2'
          #!/bin/bash
          REAL_AVDMANAGER="$(dirname "$(dirname "$(dirname "$0")")")/cmdline-tools/latest/bin/avdmanager"
          if [ -x "$REAL_AVDMANAGER" ]; then
              exec "$REAL_AVDMANAGER" "$@"
          else
              exit 1
          fi
          WRAPPER2
          chmod +x /tmp/avdmanager_wrapper.sh
          cp /tmp/avdmanager_wrapper.sh "$LEGACY_AVDMANAGER" 2>/dev/null || sudo cp /tmp/avdmanager_wrapper.sh "$LEGACY_AVDMANAGER"
          chmod +x "$LEGACY_AVDMANAGER"

      - name: Cache NDK and p4a
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer/android/platform/android-ndk-r25b
            ~/.buildozer/android/platform/python-for-android
            /home/runner/.local/share/python-for-android
          key: ndk-p4a-v14-${{ runner.os }}

      - name: Pre-clone python-for-android master
        run: |
          git clone --depth=1 -b master https://github.com/kivy/python-for-android.git /home/runner/work/python-for-android
          cd /home/runner/work/python-for-android
          git log --oneline -1

      - name: Download chatbot zip
        run: |
          gdown 'https://drive.google.com/uc?id=1_GJVvaM2wKXFAo6mDaaOIpVBhrjAQ7Uf' -O 机器人.zip
          ls -lh 机器人.zip
          unzip -o 机器人.zip -d 机器人解压/
          find 机器人解压/ -type f -exec ls -lh {} \;

      - name: Extract chatbot files
        run: |
          CHATBOT_DIR=$(find 机器人解压/ -name "李尔.py" -exec dirname {} \; | head -1)
          if [ -z "$CHATBOT_DIR" ]; then
            echo "李尔.py NOT FOUND"
            find 机器人解压/ -type f
            exit 1
          fi
          echo "Chatbot dir: $CHATBOT_DIR"
          cp -v "$CHATBOT_DIR"/*.py .
          cp -v "$CHATBOT_DIR"/*.json .
          ls -la

      - name: Rename Chinese filenames to ASCII
        run: |
          python3 -c '
          import re, os

          RENAME_MODULES = {
              "李尔": "lear_main",
              "单词": "chatbot_words",
              "数据": "chatbot_data",
              "成语": "chatbot_idioms",
          }

          FILE_RENAME = {
              "字向量_f16_pure.json": "word_vectors.json",
              "李尔.py": "lear_main.py",
              "单词.py": "chatbot_words.py",
              "数据.py": "chatbot_data.py",
              "成语.py": "chatbot_idioms.py",
          }

          print("==== Original files ====")
          for f in sorted(os.listdir(".")):
              print(f"  {f}")

          contents = {}
          for cn, en in RENAME_MODULES.items():
              src = cn + ".py"
              if os.path.exists(src):
                  with open(src, "r", encoding="utf-8") as f:
                      contents[en] = f.read()
                  os.remove(src)
                  print(f"Renamed file: {src} -> {en}.py")

          def replace_all(content):
              for cn_fn, en_fn in FILE_RENAME.items():
                  content = content.replace(cn_fn, en_fn)
              for cn, en in RENAME_MODULES.items():
                  content = re.sub(
                      r"(?m)^(\s*)import " + cn + r"\s*$",
                      r"\1import " + en + " as " + cn,
                      content
                  )
                  content = re.sub(
                      r"(?m)^(\s*)import " + cn + r" as ",
                      r"\1import " + en + " as ",
                      content
                  )
                  content = re.sub(
                      r"(?m)^(\s*)from " + cn + r" import",
                      r"\1from " + en + " import",
                      content
                  )
                  content = re.sub(
                      r"(\s)import " + cn + r"(\s|$)",
                      r"\1import " + en + " as " + cn + r"\2",
                      content
                  )
                  content = re.sub(
                      r"(\s)import " + cn + r" as ",
                      r"\1import " + en + " as ",
                      content
                  )
                  content = re.sub(
                      r"(\s)from " + cn + r" import",
                      r"\1from " + en + " import",
                      content
                  )
              return content

          for en, content in contents.items():
              content = replace_all(content)
              with open(en + ".py", "w", encoding="utf-8") as f:
                  f.write(content)
              print(f"Processed: {en}.py ({len(content)} chars)")

          for cn_fn, en_fn in FILE_RENAME.items():
              if cn_fn.endswith(".json") and os.path.exists(cn_fn):
                  os.rename(cn_fn, en_fn)
                  print(f"Renamed: {cn_fn} -> {en_fn}")

          for fname in os.listdir("."):
              if fname.endswith(".py") and not any(fname == en + ".py" for en in RENAME_MODULES.values()):
                  with open(fname, "r", encoding="utf-8") as f:
                      content = f.read()
                  new_content = replace_all(content)
                  if new_content != content:
                      with open(fname, "w", encoding="utf-8") as f:
                          f.write(new_content)
                      print(f"Modified: {fname}")
          '

      # =========================================================================
      # 【新增核心环节】自动代码手术补丁：拦截多进程 IPC 锁死、添加超时释放机制
      # =========================================================================
      - name: Auto-patch source code (Disable Multiprocessing & Add Timeout)
        run: |
          python3 -c '
          import os, re

          for fname in os.listdir("."):
              if not fname.endswith(".py"): continue
              with open(fname, "r", encoding="utf-8") as f:
                  content = f.read()

              new_content = content
              
              # 1. 强制破坏多进程开启条件，改为纯单线程
              new_content = re.sub(
                  r"self\.use_parallel\s*=\s*use_parallel",
                  "self.use_parallel = False  # [CI Auto-Patch] Disabled Multiprocessing for Android",
                  new_content
              )
              new_content = re.sub(
                  r"self\.max_workers\s*=\s*.+",
                  "self.max_workers = 1  # [CI Auto-Patch] Forced single thread",
                  new_content
              )
              
              # 2. 给所有的事件等待 .wait() 强制加上超时，防止底层异常导致无限卡顿
              new_content = new_content.replace(
                  "self._index_ready_event.wait()",
                  "self._index_ready_event.wait(timeout=15.0)  # [CI Auto-Patch] Added timeout protection"
              )

              # 如果文件被成功打上补丁，就覆写回去
              if new_content != content:
                  with open(fname, "w", encoding="utf-8") as f:
                      f.write(new_content)
                  print(f"✅ Successfully patched deadlocks in {fname}")
          '

      - name: Generate main.py (多线程与内存优化版)
        run: |
          python3 -c '
          code = """# -*- coding: utf-8 -*-
          import os, sys, traceback
          import threading

          APP_DIR = os.path.dirname(os.path.abspath(__file__))
          os.chdir(APP_DIR)
          if APP_DIR not in sys.path:
              sys.path.insert(0, APP_DIR)

          INIT_ERROR = None
          chatbot = None

          try:
              print("[main.py] Loading chatbot module...", flush=True)
              import lear_main as 李尔
              chatbot = 李尔
              print("[main.py] Module loaded successfully", flush=True)
          except Exception as e:
              INIT_ERROR = traceback.format_exc()
              print("[main.py] INIT ERROR:", INIT_ERROR, flush=True)

          from kivy.app import App
          from kivy.uix.boxlayout import BoxLayout
          from kivy.uix.scrollview import ScrollView
          from kivy.uix.label import Label
          from kivy.uix.textinput import TextInput
          from kivy.uix.button import Button
          from kivy.core.window import Window
          from kivy.graphics import Color, RoundedRectangle, Rectangle
          from kivy.metrics import dp
          from kivy.clock import Clock, mainthread

          # 优化键盘弹出时的界面挤压/遮挡问题
          Window.softinput_mode = "pan"
          Window.clearcolor = (0.95, 0.95, 0.95, 1)

          CJK_FONT = "Roboto"
          for _fp in ["/system/fonts/DroidSansFallback.ttf","/system/fonts/NotoSansCJK-Regular.ttc","/system/fonts/NotoSerifCJK-Regular.ttc","/system/fonts/NotoColorEmoji.ttf","/usr/share/fonts/truetype/chinese/NotoSansSC-Regular.ttf"]:
              if os.path.exists(_fp):
                  CJK_FONT = _fp
                  break

          COLOR_USER_BG = (0.29, 0.56, 0.89, 1)
          COLOR_BOT_BG = (1, 1, 1, 1)
          COLOR_USER_FG = (1, 1, 1, 1)
          COLOR_BOT_FG = (0.13, 0.13, 0.13, 1)
          COLOR_TITLE_BG = (0.20, 0.46, 0.79, 1)
          COLOR_ERROR_BG = (0.85, 0.20, 0.20, 1)


          class ChatBubble(Label):
              def __init__(self, text, is_user=False, is_error=False, **kwargs):
                  super().__init__(**kwargs)
                  self.text = text
                  self.is_user = is_user
                  self.font_name = CJK_FONT
                  self.font_size = dp(13) if is_error else dp(16)
                  self.size_hint_y = None
                  self.halign = "left"
                  self.valign = "middle"
                  self.padding = (dp(14), dp(10))
                  self.color = COLOR_USER_FG if is_user else COLOR_BOT_FG
                  self.bind(texture_size=self._on_texture_size)
                  self.bind(size=self._on_size)
                  bg = COLOR_ERROR_BG if is_error else (COLOR_USER_BG if is_user else COLOR_BOT_BG)
                  with self.canvas.before:
                      Color(*bg)
                      self._bg = RoundedRectangle(pos=self.pos, size=self.size, radius=[dp(12)])
                  self.bind(pos=self._update_bg, size=self._update_bg)
                  
              def _on_texture_size(self, instance, value):
                  self.height = value[1] + dp(20)
                  
              def _on_size(self, instance, value):
                  # 【修复致命内存循环Bug】仅在宽度真发生变化时才更新 text_size，防止无限套娃卡死主线程
                  new_width = max(value[0] - dp(28), dp(50))
                  if getattr(self, "_last_width", None) != new_width:
                      self._last_width = new_width
                      self.text_size = (new_width, None)
                      
              def _update_bg(self, *args):
                  self._bg.pos = self.pos
                  self._bg.size = self.size


          class MessageRow(BoxLayout):
              def __init__(self, text, is_user=False, is_error=False, **kwargs):
                  super().__init__(**kwargs)
                  self.orientation = "horizontal"
                  self.size_hint_y = None
                  self.spacing = dp(6)
                  self.padding = (dp(8), dp(2))
                  bubble = ChatBubble(text, is_user=is_user, is_error=is_error, size_hint_x=0.92)
                  spacer = BoxLayout(size_hint_x=0.08)
                  if is_user:
                      self.add_widget(spacer)
                      self.add_widget(bubble)
                  else:
                      self.add_widget(bubble)
                      self.add_widget(spacer)
                  bubble.bind(height=lambda inst, val: setattr(self, "height", val + dp(8)))
                  self.height = bubble.height + dp(8)


          class ChatApp(App):
              def __init__(self, **kwargs):
                  super().__init__(**kwargs)
                  self.session_id = None
                  self.is_processing = False

              def build(self):
                  root = BoxLayout(orientation="vertical")
                  title = Label(text="李尔", font_name=CJK_FONT, font_size=dp(22), bold=True, size_hint=(1, None), height=dp(54), color=(1, 1, 1, 1))
                  with title.canvas.before:
                      Color(*COLOR_TITLE_BG)
                      title._bg = Rectangle(pos=title.pos, size=title.size)
                  title.bind(pos=lambda inst, val: setattr(title._bg, "pos", val), size=lambda inst, val: setattr(title._bg, "size", val))
                  root.add_widget(title)

                  self.scroll = ScrollView(size_hint=(1, 1))
                  self.chat_layout = BoxLayout(orientation="vertical", size_hint_y=None, spacing=dp(6), padding=[dp(10), dp(10), dp(10), dp(10)])
                  self.chat_layout.bind(minimum_height=self.chat_layout.setter("height"))
                  self.scroll.add_widget(self.chat_layout)
                  root.add_widget(self.scroll)

                  input_area = BoxLayout(orientation="horizontal", size_hint=(1, None), height=dp(58), spacing=dp(8), padding=[dp(10), dp(6), dp(10), dp(6)])
                  with input_area.canvas.before:
                      Color(1, 1, 1, 1)
                      input_area._bg = Rectangle(pos=input_area.pos, size=input_area.size)
                  input_area.bind(pos=lambda inst, val: setattr(input_area._bg, "pos", val), size=lambda inst, val: setattr(input_area._bg, "size", val))
                  
                  # 【修复拼音回车闪退Bug】改为 multiline=True 避免中文输入法敲击回车确认拼音时意外触发发送
                  self.text_input = TextInput(hint_text="输入消息…", font_name=CJK_FONT, font_size=dp(16), multiline=True, size_hint=(1, 1), padding=[dp(10), dp(12), dp(10), dp(10)])
                  input_area.add_widget(self.text_input)
                  
                  self.send_btn = Button(text="发送", font_name=CJK_FONT, font_size=dp(16), size_hint=(None, 1), width=dp(82), color=(1, 1, 1, 1), background_color=COLOR_TITLE_BG, background_normal="")
                  self.send_btn.bind(on_release=self.send_message)
                  input_area.add_widget(self.send_btn)
                  root.add_widget(input_area)

                  Clock.schedule_once(self.init_chat_session, 0)
                  return root

              def init_chat_session(self, dt):
                  if INIT_ERROR:
                      self._on_init_error(INIT_ERROR)
                      return
                  # 【UI流畅度优化】使用后台线程异步加载对话模型，避免启动时卡死页面
                  threading.Thread(target=self._init_session_thread, daemon=True).start()

              def _init_session_thread(self):
                  try:
                      result = chatbot.web_start_session()
                      self.session_id = result.get("session_id")
                      msgs = result.get("messages", [])
                      self._on_message_success(msgs)
                  except Exception as e:
                      err = traceback.format_exc()
                      self._on_init_error(err)

              @mainthread
              def _on_init_error(self, err):
                  self.add_error_message("【会话初始化失败】")
                  for line in err.split("\\n"):
                      if line.strip():
                          self.add_error_message(line)

              def add_bot_message(self, text):
                  text = (text or "").strip()
                  if not text: return
                  self.chat_layout.add_widget(MessageRow(text, is_user=False))
                  self._scroll_to_bottom()

              def add_user_message(self, text):
                  text = (text or "").strip()
                  if not text: return
                  self.chat_layout.add_widget(MessageRow(text, is_user=True))
                  self._scroll_to_bottom()

              def add_error_message(self, text):
                  self.chat_layout.add_widget(MessageRow(text, is_user=False, is_error=True))
                  self._scroll_to_bottom()

              def _scroll_to_bottom(self):
                  Clock.schedule_once(lambda dt: setattr(self.scroll, "scroll_y", 0), 0.1)

              def send_message(self, instance):
                  if INIT_ERROR or self.is_processing:
                      return
                  text = (self.text_input.text or "").strip()
                  if not text:
                      return
                  self.add_user_message(text)
                  self.text_input.text = ""
                  self.text_input.focus = False  # 【修复键盘丢失焦点Bug】清除焦点使键盘收起，避免之后点不出键盘
                  
                  self.is_processing = True
                  self.send_btn.text = "发送中"
                  self.send_btn.background_color = (0.5, 0.5, 0.5, 1)
                  
                  # 【修复发送黑屏Bug】异步发送消息处理，防止主线程阻塞导致APP无响应
                  threading.Thread(target=self._process_message_thread, args=(text,), daemon=True).start()

              def _process_message_thread(self, text):
                  try:
                      result = chatbot.web_process_message(text, self.session_id)
                      self.session_id = result.get("session_id")
                      replies = result.get("replies", [])
                      self._on_message_success(replies)
                  except Exception as e:
                      err = traceback.format_exc()
                      self._on_message_error(err)

              @mainthread
              def _on_message_success(self, replies):
                  self.is_processing = False
                  self.send_btn.text = "发送"
                  self.send_btn.background_color = COLOR_TITLE_BG
                  for reply in replies:
                      self.add_bot_message(reply)

              @mainthread
              def _on_message_error(self, err):
                  self.is_processing = False
                  self.send_btn.text = "发送"
                  self.send_btn.background_color = COLOR_TITLE_BG
                  self.add_error_message("【发送消息出错】")
                  for line in err.split("\\n"):
                      if line.strip():
                          self.add_error_message(line)

          if __name__ == "__main__":
              ChatApp().run()
          """
          with open("main.py", "w", encoding="utf-8") as f:
              f.write(code)
          print("main.py generated (async & multi-thread optimized version)")
          '

      - name: Generate buildozer.spec
        run: |
          python3 -c '
          spec = """[app]
          title = 李尔聊天机器人
          package.name = mychatbot
          package.domain = org.lear
          source.dir = .
          source.include_exts = py,png,jpg,jpeg,kv,atlas,json,ttf,otf,txt,cfg,ini
          version = 1.0.3
          requirements = python3,kivy
          orientation = portrait
          fullscreen = 0
          android.permissions = INTERNET
          android.minapi = 21
          android.api = 34
          android.ndk = 25b
          android.accept_sdk_license = True
          android.allow_backup = 1
          presplash.duration = 3
          p4a.branch = master
          p4a.source_dir = /home/runner/work/python-for-android
          no-compile-pyo = False

          [buildozer]
          log_level = 2
          warn_on_root = 1
          """
          with open("buildozer.spec", "w", encoding="utf-8") as f:
              f.write(spec)
          print("buildozer.spec generated")
          '

      - name: Build APK
        run: |
          echo "==== Build APK ===="
          echo "JAVA_HOME=$JAVA_HOME"
          java -version 2>&1
          ls "$ANDROID_HOME/build-tools/" 2>&1
          ls "$ANDROID_HOME/platforms/" 2>&1
          echo "---- Files to package ----"
          ls -lh *.py *.json 2>&1

          set -o pipefail
          buildozer -v android debug 2>&1 | tee build.log
          EXIT_CODE=${PIPESTATUS[0]}

          echo "==== Build exit code: $EXIT_CODE ===="
          ls -lh bin/ 2>/dev/null || echo "bin/ not found"

          if ! ls bin/*.apk 2>/dev/null; then
            echo "APK NOT GENERATED"
            exit 1
          fi

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: 李尔聊天机器人-APK
          path: bin/*.apk
          retention-days: 30
          if-no-files-found: error

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: |
            build.log
            .buildozer/**/*.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Print error context
        if: failure()
        run: |
          echo "==== Last 200 lines of build.log ===="
          tail -n 200 build.log 2>/dev/null || echo "no build.log"
          echo ""
          echo "==== Error context ===="
          if [ -f build.log ]; then
            grep -n -iE "error|fail|exception|traceback|not found|invalid|cannot|denied|license" build.log | head -30 | while read line; do
              LINE_NUM=$(echo "$line" | cut -d: -f1)
              echo ""
              echo "=== Error at line $LINE_NUM ==="
              START=$((LINE_NUM - 15))
              END=$((LINE_NUM + 15))
              if [ $START -lt 1 ]; then START=1; fi
              sed -n "${START},${END}p" build.log
              echo "=== end ==="
            done
          fi
